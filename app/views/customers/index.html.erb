<% if flash[:notice] %>
  <p> <%= flash[:notice] %> </p>
<% end %>

<h2>Listando Clientes</h2>

<ul>
  <% @customers.each do |customer| %>
  <li>
    <%= customer.name %>
  </li>
  <% end %>
</ul>

<%= link_to "Novo Cliente", new_customer_path %>